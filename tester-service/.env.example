PROJECT_NAME=tester
DEBUG=true
# Optional: explicit BGP YAML for loader (Docker Compose mounts ./tester-service/config → /opt/config)
# BGP_CONFIG_FILE=/opt/config/bgp_config.yaml
# Browser upload from controller UI (default ports)
# CORS_ORIGINS=http://localhost:8001,http://127.0.0.1:8001